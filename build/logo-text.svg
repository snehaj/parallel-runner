<!-- PLACEHOLDER wordmark — regenerated as a live <text> element, not glyph-outline
     paths like the original. This renders fine in a browser/README but is NOT
     suitable for use as an actual app icon/build resource (font may not be
     present at render time in every context). Needs a proper redo (font
     converted to path outlines) as a follow-up design task. -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 378 84" fill="none">
  <!-- Black background -->
  <rect width="378" height="84" rx="14" fill="#000000"/>

  <!-- Logo icon -->
  <g stroke="#2ec8ff" stroke-width="4" stroke-linecap="round" stroke-linejoin="round" transform="translate(20, 14)">
    <line x1="10" y1="6" x2="10" y2="50"/>
    <line x1="22" y1="6" x2="22" y2="50"/>
    <path d="M30 8 H47 V24 H30"/>
    <path d="M49 32 H32 V48 H49"/>
  </g>

  <!-- "Paralell Runner" -->
  <text x="88" y="52" font-family="'JetBrains Mono', monospace" font-weight="600" font-size="24" fill="#2ec8ff">Paralell Runner</text>
</svg>
